import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  breadcrumbs: {
    width: "100%",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  breadcrumbsLink: { fontSize: 20 },
}));

export default useStyles;
