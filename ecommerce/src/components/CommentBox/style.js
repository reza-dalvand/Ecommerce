import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  comments: { width: "100%" },
  inputs: { width: "100%" },
  input: { width: "100%" },
}));

export default useStyles;
